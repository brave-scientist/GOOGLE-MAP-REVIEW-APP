# GLM 5.2 AUDIT PROMPT — Full Multilayer E2E Application Audit
# ReviewReply-Lite — Commercial Launch Readiness Assessment
#
# Instructions for GLM 5.2:
# This is a READ + TEST + REPORT session. You are acting as a senior QA engineer
# conducting a full pre-launch audit. You will read every file, run every terminal
# check, trace every route, test every interactive element, and produce a single
# structured audit report as your final output.
#
# Operating rules for this session:
# - Work through all 18 layers in order without stopping between layers.
# - Do not ask for confirmation or approval between layers — proceed continuously
#   from Layer 1 all the way through to the final AUDIT-REPORT.md write and commit.
# - "Layer N complete" is not a stopping point. Move immediately to Layer N+1.
# - Auto-approved for this session: all file reads, terminal commands (curl, grep,
#   find, pnpm build, tsc), test runs, and writing/committing AUDIT-REPORT.md.
# - NOT approved: editing any application source file during this session.
#   This session is audit-only. Find, document, and classify findings.
#   Do not fix anything. Fixes happen in a separate dedicated session after
#   the founder reviews this report.
# - If you approach your context window limit before finishing all 18 layers,
#   write whatever you have to AUDIT-REPORT.md immediately, note exactly which
#   layer you stopped at and what the next unfinished check was, commit it, and
#   stop. Do not summarise loosely — be precise about where you are so the next
#   session resumes from the exact right place.
# - GLM 5.2 specific note: day/layer numbers are sequence markers, not pause points.
#   Do not stop after completing a layer and wait for a "continue" instruction.
#   The instruction to continue through all layers is given here, once, and applies
#   to the entire session.
#
# Before starting Layer 1: start the dev server with `pnpm dev` and confirm
# the app is responding at http://localhost:3001 before proceeding.
# Final deliverable: AUDIT-REPORT.md in the project root, committed to git.

---

## AUDIT SCOPE

You are auditing the following layers, in this order:

1. File system & code structure integrity
2. Build & compilation health  
3. Environment & configuration
4. Public marketing routes (unauthenticated)
5. Authentication flows
6. Onboarding flow
7. Core app routes (authenticated)
8. Every interactive element on every page
9. Every navigation link and internal route
10. Every API endpoint
11. Data flow & state management
12. Mock-mode integrity
13. Mobile & responsive layout
14. Performance & load behaviour
15. Security surface check
16. Commercial launch readiness verdict

For each finding, classify it as:
- 🔴 BLOCKER — must fix before any real user touches this (broken flow, data loss, security issue)
- 🟡 MAJOR — significantly damages UX or conversion, fix before commercial launch
- 🟠 MINOR — noticeable issue but doesn't break core flows, fix within first week post-launch
- 🔵 POLISH — cosmetic or minor UX improvement, fix when time permits
- ✅ PASS — working correctly, no action needed

---

## LAYER 1 — File System & Code Structure Integrity

Run these checks and record findings:

```bash
# 1.1 — Verify the exact folder structure matches Implementation-Plan.md Section 1
# List all files in app/ and lib/ recursively
find app -type f | sort
find lib -type f | sort
find components -type f | sort
find scripts -type f | sort
```

Check and answer each question:
- Does `lib/types.ts` exist and export all interfaces from Product-Roadmap.md Section 2?
- Does `lib/auth.ts` exist with a `getSession()` function?
- Does `lib/db.ts` exist and implement the dual data-layer (LocalStore + SupabaseStore)?
- Does `lib/supabase/supabase-store.ts` exist?
- Do all 6 integration files exist: `lib/integrations/google-business-profile.ts`, `lib/integrations/facebook-graph.ts`, `lib/integrations/twilio.ts`, `lib/integrations/resend.ts`, `lib/integrations/stripe.ts`, `lib/integrations/mock-data.ts`?
- Does `lib/ai/draft-reply.ts` exist?
- Do all 3 Inngest job files exist: `lib/inngest/poll-reviews.ts`, `lib/inngest/generate-reply-draft.ts`, `lib/inngest/send-review-request.ts`?
- Does `lib/parsing/paste-list-parser.ts` exist?
- Does `MOCK-TO-REAL.md` exist in the project root?
- Does `Decision-Log.md` exist and have entries?
- Does `supabase/migrations/0001_init.sql` exist?
- Does `supabase/migrations/0002_agency_support.sql` exist?
- Does `.env.example` exist with all required keys from Implementation-Plan.md Section 2?
- Does `.vscode/settings.json` exist with CSS validator disabled?
- Do all marketing routes exist: `app/(marketing)/page.tsx`, `/login`, `/signup`, `/terms`, `/privacy`, `/vs/birdeye`, `/vs/podium`?
- Do all app routes exist: `app/(app)/dashboard/page.tsx`, `/reviews`, `/requests`, `/settings`, `/billing`, `/onboarding`?
- Do all API routes exist per the Implementation-Plan route list?
- Does `app/r/[token]/page.tsx` exist for click tracking?

Record every missing file as 🔴 BLOCKER if it's a core route or integration, 🟡 MAJOR if it's a supporting component.

---

## LAYER 2 — Build & Compilation Health

```bash
# 2.1 — TypeScript check — zero errors required
npx tsc --noEmit 2>&1

# 2.2 — Full production build
pnpm build 2>&1

# 2.3 — Check for any console.error or unhandled promise rejections in dev mode
# Start dev server and check terminal output for any startup errors
pnpm dev 2>&1 &
sleep 8
```

Questions to answer:
- Does `tsc --noEmit` complete with zero errors?
- Does `pnpm build` complete successfully with all routes compiled?
- How many routes does the build report? (Should be 24 minimum per the build summary)
- Are there any warnings in the build output that could indicate runtime issues?
- Are there any `any` type usages that bypass type safety in critical paths (auth, billing, data)?

Record each error as 🔴 BLOCKER, each warning as 🟡 MAJOR or 🟠 MINOR depending on context.

---

## LAYER 3 — Environment & Configuration

Open and read `.env.example`. Then read `.env.local` (if present — do not log its contents to any output file, just verify the keys exist).

Check and answer:
- Is `USE_MOCKS=true` set correctly in `.env.local`?
- Is `NEXT_PUBLIC_APP_URL=http://localhost:3001` set?
- Are all required env variable keys present in `.env.example` matching Implementation-Plan.md Section 2?
- Are there any keys in `.env.local` that are NOT in `.env.example` (undocumented secrets)?
- Is `.env.local` correctly listed in `.gitignore`?
- Is there any hardcoded `localhost:3000` remaining anywhere in the codebase?

```bash
# Check for any remaining localhost:3000 references
grep -r "localhost:3000" . --include="*.ts" --include="*.tsx" --include="*.js" --include="*.json" 2>/dev/null
```

- Is `package.json` dev script set to `--port 3001`?
- Does `.vscode/settings.json` have `css.validate: false`?

---

## LAYER 4 — Public Marketing Routes (Unauthenticated)

For each route below, open it (use curl or document the check), and answer every question.

### 4.1 — Homepage: http://localhost:3001/

Navigation audit:
- [ ] Does the page load without a blank screen or error?
- [ ] Is the sticky header present and visible?
- [ ] Logo renders correctly with the brass diamond mark?
- [ ] Nav links visible on desktop: "How it works", "Features", "Pricing", "FAQ"?
- [ ] Nav links hidden on mobile viewport (≤860px)?
- [ ] "Log in" ghost button in header — does it link to /login?
- [ ] "Start free trial" brass button in header — does it link to /pricing section or /signup?
- [ ] Clicking logo — does it link to /?

Hero section:
- [ ] Eyebrow label renders?
- [ ] Headline renders with brass accent text?
- [ ] Subheadline renders?
- [ ] "Start your 14-day free trial" button — where does it link? Is the target correct?
- [ ] "See how it works" button — does it scroll to #how section?
- [ ] Hero note text renders?
- [ ] Demo card renders correctly?
- [ ] Typewriter animation starts automatically?
- [ ] Typewriter animation loops correctly after completing?
- [ ] "✓ Posted to Google" check appears after typing completes?
- [ ] Demo card is responsive — does it stack below text on mobile?

Scroll animations (if implemented):
- [ ] Do reveal animations fire on scroll?
- [ ] Do they fire on first load for hero elements?
- [ ] Do any elements stay invisible (stuck in opacity:0) due to animation not firing?

Logo carousel (if implemented):
- [ ] Does the carousel render with business logos?
- [ ] Does it auto-scroll continuously?
- [ ] Does scrolling pause on hover?
- [ ] Are the gradient fade masks on left/right edges present?
- [ ] Is the loop seamless (no jump at the seam)?

Activity ticker (if implemented):
- [ ] Does it appear after ~8 seconds?
- [ ] Does it auto-dismiss after ~5 seconds?
- [ ] Is the X dismiss button functional?
- [ ] Does it show the next notification after ~30 seconds?

Problem/Stats section:
- [ ] Three stat cards render?
- [ ] Do stat counters animate on scroll?
- [ ] Are placeholder tags visible for stats that need real sourced figures?

How it works section (#how):
- [ ] Four step cards render?
- [ ] Step numbers, titles, descriptions all present?
- [ ] Anchor link from "See how it works" button correctly scrolls here?

Before/After section (if implemented):
- [ ] Two tabs render: "Without ReviewReply-Lite" and "With ReviewReply-Lite"?
- [ ] Default tab is "Without"?
- [ ] Clicking "With" tab switches content?
- [ ] Transition is smooth?
- [ ] All 5 steps visible in each tab?

Live demo section (if implemented):
- [ ] Star rating picker renders and is clickable (1-5 stars)?
- [ ] Business type dropdown renders with all 5 options?
- [ ] Review textarea pre-fills with contextual example?
- [ ] "Generate Reply" button triggers typewriter animation?
- [ ] Reply output matches the correct template for the selected rating + type?
- [ ] NO network requests fire when "Generate Reply" is clicked? (Check DevTools → Network)
- [ ] "Post to Google" non-functional button shows tooltip directing to signup?

Features section:
- [ ] Three feature rows render?
- [ ] Images/mockup placeholders render (not broken img tags)?
- [ ] Placeholder tags visible on mock screenshots?
- [ ] Feature list items render with arrow markers?
- [ ] Alternating layout correct (left/right/left) on desktop?
- [ ] Stacks to single column on mobile?

Competitor comparison section (if implemented):
- [ ] Comparison table/grid renders?
- [ ] ReviewReply-Lite column is highlighted?
- [ ] Pricing figures are correct: $29/mo Starter, $59/mo Pro?
- [ ] Competitor pricing figures are accurate (not fabricated)?

Pricing section:
- [ ] Both cards render: Starter ($29/mo) and Pro ($59/mo)?
- [ ] Starter feature list accurate per locked spec?
- [ ] Pro feature list accurate per locked spec?
- [ ] "Most Popular" badge on Pro card?
- [ ] Overage pricing note ($0.05/request) visible below cards?
- [ ] Monthly/annual toggle renders (if implemented)?
- [ ] Both "Start free trial" buttons present?
- [ ] Where do the trial buttons link? Record the URL.

Review platform badges (if implemented):
- [ ] G2, Capterra, Trustpilot, ProductHunt badge placeholders render?
- [ ] Each badge links to the correct external URL?
- [ ] No broken SVG icons?

Testimonials section:
- [ ] Testimonial cards render (even if placeholder content)?
- [ ] Carousel auto-rotates if carousel is implemented?
- [ ] Dot navigation present and functional?
- [ ] Placeholder comment exists in JSX marking these as demo content?

FAQ section:
- [ ] All FAQ items render?
- [ ] Clicking a question expands the answer?
- [ ] Clicking an open question collapses it?
- [ ] Opening one FAQ collapses any previously open one?
- [ ] All 8 questions present (5 original + 3 added in enhancement pass)?

Final CTA section:
- [ ] Renders correctly?
- [ ] Button links to pricing section or signup?

Footer:
- [ ] Four columns render on desktop?
- [ ] Two columns on tablet, single on mobile?
- [ ] All nav links in footer are functional (not broken hrefs)?
- [ ] Terms of Service link goes to /terms?
- [ ] Privacy Policy link goes to /privacy?
- [ ] "/vs/birdeye" link goes to /vs/birdeye?
- [ ] "/vs/podium" link goes to /vs/podium?
- [ ] Support email placeholder visible and tagged?
- [ ] Copyright year is 2026?

### 4.2 — Terms of Service: http://localhost:3001/terms
- [ ] Page loads without 404?
- [ ] Has content (even placeholder)?
- [ ] Has a back/home link?
- [ ] Renders correctly on mobile?

### 4.3 — Privacy Policy: http://localhost:3001/privacy
- [ ] Page loads without 404?
- [ ] Has content (even placeholder)?
- [ ] Has a back/home link?
- [ ] Renders correctly on mobile?

### 4.4 — Birdeye comparison: http://localhost:3001/vs/birdeye
- [ ] Page loads without 404?
- [ ] Accurate Birdeye pricing (~$299/mo)?
- [ ] Accurate contract information (annual required)?
- [ ] Comparison table present with correct data?
- [ ] ReviewReply-Lite pricing correct ($29/mo)?
- [ ] "$270/mo cheaper" callout present?
- [ ] CTA button to signup present?
- [ ] No fabricated competitor claims?

### 4.5 — Podium comparison: http://localhost:3001/vs/podium
- [ ] Page loads without 404?
- [ ] Accurate Podium pricing (~$289/mo)?
- [ ] Comparison table present?
- [ ] "$260/mo cheaper" callout present?
- [ ] CTA button present?
- [ ] No fabricated competitor claims?

---

## LAYER 5 — Authentication Flows

### 5.1 — Login page: http://localhost:3001/login
- [ ] Page loads without 404?
- [ ] Page matches the pine-green design system (not a generic grey form)?
- [ ] Logo and value prop line visible at top?
- [ ] Email/password fields render?
- [ ] "Continue with Google" button renders with Google logo?
- [ ] "Continue with phone number" option renders?
- [ ] In mock mode: any email/password combination logs in successfully?
- [ ] In mock mode: Google button logs in immediately as demo user?
- [ ] In mock mode: phone number flow shows OTP step?
- [ ] In mock mode: any 6-digit OTP on phone flow logs in successfully?
- [ ] After successful login, where does the user land? (Should be /dashboard)
- [ ] Is there a link to signup from the login page?
- [ ] "Forgot password" link present? (Even if non-functional in mock mode)
- [ ] Form submits on Enter key?
- [ ] Loading state shown while login processes?
- [ ] Error state shown for failed login attempt (in mock mode, simulate with intentional failure if possible)?

### 5.2 — Signup page: http://localhost:3001/signup
- [ ] Page loads without 404?
- [ ] Email/password signup fields render?
- [ ] Google OAuth signup button renders?
- [ ] Phone signup option renders?
- [ ] In mock mode: signup creates a session and redirects to /onboarding?
- [ ] Is there a link to login from the signup page?
- [ ] Terms of Service checkbox or acknowledgement text present? (Required before collecting real user data)
- [ ] Password strength indicator or minimum length hint?

### 5.3 — Session persistence
- [ ] After login, refreshing the page keeps the user logged in?
- [ ] After login, navigating directly to /dashboard works without redirect to /login?
- [ ] Navigating to /login while already logged in — where does it go? (Should redirect to /dashboard)

### 5.4 — Logout
- [ ] Is there a logout option anywhere in the app? (Settings or nav menu)
- [ ] Does logout clear the session?
- [ ] After logout, does navigating to /dashboard redirect to /login?

---

## LAYER 6 — Onboarding Flow: http://localhost:3001/onboarding

- [ ] Page loads without 404?
- [ ] Step 1 visible: business name input, category dropdown, timezone field?
- [ ] Category dropdown has all 6 options: restaurant, salon, dental, retail, contractor, other?
- [ ] Timezone auto-detected from browser?
- [ ] Step 2 visible: brand voice textarea?
- [ ] Brand voice placeholder text matches spec: "Tell us how you talk to customers..."?
- [ ] Step 3 (or separate step): "Connect Google Business Profile" button?
- [ ] In mock mode: clicking Connect Google shows a mock location picker (not a real OAuth redirect)?
- [ ] Mock location picker shows plausible location options to select?
- [ ] Step for Facebook Page connection present?
- [ ] Facebook connection marked as optional?
- [ ] Progress indicator visible (step 1 of N)?
- [ ] Can the user go back to a previous step?
- [ ] Completing all steps redirects to /dashboard?
- [ ] If user skips onboarding and goes directly to /dashboard, is there a guided checklist visible?
- [ ] Onboarding is not shown again after completion (on next login)?
- [ ] What happens if the user closes the browser mid-onboarding and comes back?

---

## LAYER 7 — Dashboard: http://localhost:3001/dashboard

Navigation shell:
- [ ] Authenticated layout renders (sidebar or top nav)?
- [ ] All nav items present: Dashboard, Reviews, Requests, Settings, Billing?
- [ ] Active nav item is highlighted?
- [ ] User email or name visible in the nav?
- [ ] Logout button accessible from nav?
- [ ] Nav collapses or adapts correctly on mobile?

Dashboard content:
- [ ] Page loads with seeded mock data (not empty state)?
- [ ] Business name displayed correctly?
- [ ] Key stats visible: total reviews, average rating, requests sent, replies posted?
- [ ] Review velocity sparkline chart renders (if implemented)?
- [ ] Negative review alert banner renders if there are unanswered 1-2 star reviews?
- [ ] Alert banner "Reply now →" link navigates to the reviews page filtered to negative?
- [ ] Recent activity feed or recent reviews list visible?
- [ ] Each item in recent list is clickable and goes to the correct detail?
- [ ] Empty state renders correctly if all reviews are replied (no broken layout)?

---

## LAYER 8 — Reviews Page: http://localhost:3001/reviews

- [ ] Page loads without 404?
- [ ] Review list renders with seeded mock reviews?
- [ ] Each review card shows: source icon (Google/Facebook), star rating, reviewer name, review text, date, status badge?
- [ ] Reviews sorted newest first by default?
- [ ] Filter by source (Google/Facebook) works?
- [ ] Filter by status (new/replied/ignored) works?
- [ ] Filter by star rating works?
- [ ] For each review with status 'draft_generated':
  - [ ] AI draft text visible in an editable textarea?
  - [ ] Draft text follows the correct tone rules (warm for 5-star, resolution-focused for 1-2 star)?
  - [ ] "Post Reply" button present?
  - [ ] "Regenerate" button present?
  - [ ] Clicking "Post Reply" marks review as replied and updates status badge?
  - [ ] Clicking "Regenerate" replaces draft text with a new variation?
  - [ ] Edited text in textarea is preserved if user types in it before posting?
  - [ ] Posted reply shows the actual posted text, not the original draft?
- [ ] For reviews with status 'replied': shows the posted reply text?
- [ ] For reviews with status 'new': shows a "Generate draft" button?
- [ ] For reviews with status 'ignored': shows an "Unignore" or "Reply" option?
- [ ] "Ignore" action available on reviews (mark as acknowledged without replying)?
- [ ] Pagination or infinite scroll if more than N reviews?
- [ ] Empty state if no reviews exist (correct message, not broken layout)?
- [ ] Loading state while reviews fetch?

---

## LAYER 9 — Requests Page: http://localhost:3001/requests

- [ ] Page loads without 404? (This was a 🔴 BLOCKER in the previous session — verify fixed)
- [ ] Quick-add form visible at the top of the page?
- [ ] Quick-add form has: name field (required), phone field (required), email field (optional)?
- [ ] Quick-add form is mobile-friendly with large touch targets?
- [ ] Submitting quick-add with valid name + phone immediately queues a send?
- [ ] After quick-add submit: new customer appears in the request list with status "queued" or "sent"?
- [ ] Submitting quick-add with empty name shows validation error?
- [ ] Submitting quick-add with empty phone shows validation error?
- [ ] Submitting quick-add with invalid phone format shows validation error?
- [ ] Paste-a-list section present below quick-add?
- [ ] Paste-a-list textarea accepts multi-line input?
- [ ] "Parse" button triggers parsing of pasted text?
- [ ] Parsed preview shows a table of extracted contacts?
- [ ] Loosely formatted input parses correctly (test with: "Jane Smith, 555-1234" and "Jane Smith - 555-1234" and "Jane 5551234")?
- [ ] Unparseable lines shown separately with an error indicator?
- [ ] "Send to All" button on parsed preview queues all valid contacts?
- [ ] CSV upload section present (collapsed or secondary)?
- [ ] CSV upload accepts a .csv file?
- [ ] CSV upload shows a preview before sending?
- [ ] Running request list visible showing all past requests?
- [ ] Each request shows: customer name, channel (SMS/email), status badge, sent time?
- [ ] Status badges: queued, sent, failed, clicked — all styled distinctly?
- [ ] Click tracking: is there a column or indicator for "clicked" status?
- [ ] Message template editor accessible from this page or from Settings?
- [ ] What happens when the monthly request limit (100 Starter / 300 Pro) is reached? Is there an upgrade prompt?
- [ ] Empty state if no requests sent yet (not a broken layout)?

---

## LAYER 10 — Settings Page: http://localhost:3001/settings

- [ ] Page loads without 404?
- [ ] Business name editable?
- [ ] Business category editable?
- [ ] Timezone editable?
- [ ] Brand voice notes editable and saveable?
- [ ] "Save" button present — does it actually persist changes?
- [ ] After saving and refreshing, are changes retained?
- [ ] Auto-post toggle present?
- [ ] Auto-post toggle default is OFF?
- [ ] Auto-post toggle state persists after page refresh?
- [ ] When auto-post is ON: is it clear that only 4-5 star reviews auto-post?
- [ ] Notification preferences: email on new review toggle?
- [ ] Notification preferences: daily digest toggle?
- [ ] Google Business Profile connection status shown?
- [ ] Facebook Page connection status shown?
- [ ] Disconnect/reconnect options for each platform?
- [ ] Message template editor present (for review request messages)?
- [ ] Template supports merge fields: {customer_name}, {business_name}, {review_link}?
- [ ] Template changes saveable?
- [ ] Danger zone / account deletion option? (Even if just a placeholder with a "contact support" instruction)

---

## LAYER 11 — Billing Page: http://localhost:3001/billing

- [ ] Page loads without 404? (This was a 🔴 BLOCKER — verify fixed)
- [ ] Current plan name displayed (Starter or Pro)?
- [ ] Current plan price displayed ($29 or $59)?
- [ ] Next billing date displayed?
- [ ] Trial status displayed if in trial period?
- [ ] Review request usage: X of 100 used (Starter) or X of 300 used (Pro) this month?
- [ ] "Upgrade to Pro" button visible on Starter plan?
- [ ] Clicking upgrade — what happens in mock mode? (Should simulate upgrade, not crash)
- [ ] "Update payment method" button present?
- [ ] "View invoices" section or button present?
- [ ] Self-serve cancel button present?
- [ ] Cancel does NOT require a support ticket — it should be completable in the UI?
- [ ] Clicking cancel — what happens in mock mode? Shows confirmation dialog?
- [ ] After simulated cancellation, does subscription status update to "canceled"?
- [ ] Overage pricing ($0.05/request) mentioned on this page?

---

## LAYER 12 — API Endpoint Audit

For each API endpoint, make a request (use curl or fetch from browser DevTools) and verify:

```bash
BASE="http://localhost:3001"

# Auth
curl -s -o /dev/null -w "%{http_code}" $BASE/api/auth

# Businesses
curl -s -o /dev/null -w "%{http_code}" $BASE/api/businesses

# Reviews (need a business ID — use the seeded mock business ID)
# First get the business ID from the dashboard or database
BIDS=$(curl -s $BASE/api/businesses | grep -o '"id":"[^"]*"' | head -1)
echo "Business IDs found: $BIDS"

# Review requests
curl -s -o /dev/null -w "%{http_code}" -X POST $BASE/api/businesses/mock-biz-id/review-requests/quick-add \
  -H "Content-Type: application/json" \
  -d '{"name":"Test Customer","phone":"555-0100"}'

# Billing
curl -s -o /dev/null -w "%{http_code}" $BASE/api/billing/checkout
curl -s -o /dev/null -w "%{http_code}" $BASE/api/billing/manage

# Inngest
curl -s -o /dev/null -w "%{http_code}" $BASE/api/inngest

# Click tracking redirect
curl -s -o /dev/null -w "%{http_code}" $BASE/r/fake-token-123
```

For each endpoint, record:
- HTTP status code returned
- Whether it returns the expected response shape
- Whether unauthenticated requests are correctly rejected (401/403) for protected endpoints
- Whether the endpoint crashes (500) on any basic input

---

## LAYER 13 — Data Flow & State Management

Trace these complete data flows end-to-end and verify each step:

### Flow A — New review appears and gets a reply posted
1. Trigger the mock poll-reviews Inngest job via `GET /api/dev/trigger` (or however it's triggered manually)
2. Verify new mock reviews appear in the reviews table (check the reviews page)
3. Verify AI draft is generated for new reviews (check draft_generated status)
4. Verify owner notification would fire (check console log for email mock)
5. Post a reply via the UI
6. Verify review status changes to 'replied'
7. Verify the posted reply text appears on the review card
8. Verify the reply appears in review_replies table (check via API or LocalStore file)

### Flow B — Quick-add customer → SMS send
1. Submit quick-add form with test name + phone
2. Verify review_requests row created with status 'queued'
3. Verify send job fires (check console log for Twilio mock message)
4. Verify status updates to 'sent'
5. Navigate to /r/[token] (use the token from the request) 
6. Verify status updates to 'clicked'
7. Verify redirect goes to the Google review URL (or a placeholder)

### Flow C — Signup → Onboarding → Dashboard with data
1. Clear session (logout)
2. Signup as a new mock user
3. Complete onboarding (connect mock business, set brand voice)
4. Verify dashboard loads with the correct business data
5. Verify the Inngest poll job can be triggered for the new business
6. Verify reviews appear after polling

### Flow D — Billing lifecycle
1. Start on free trial (check billing page shows trial status)
2. Simulate trial end / upgrade to Pro (use billing page UI)
3. Verify plan shows as Pro on billing page
4. Verify Pro features are now accessible (Facebook reviews section, SMS requests)
5. Simulate cancellation
6. Verify subscription status shows canceled
7. Verify appropriate access restriction (or grace period message)

---

## LAYER 14 — Mock-Mode Integrity Check

Read `MOCK-TO-REAL.md` fully. Then verify:

- [ ] Every file listed in MOCK-TO-REAL.md actually exists at the path listed?
- [ ] Every env variable listed in MOCK-TO-REAL.md is present in `.env.example`?
- [ ] Setting `USE_MOCKS=false` and leaving other env vars blank — does the app fail gracefully (clear error message) rather than crash silently?
- [ ] Is there any place in the code where a real API call is made even when `USE_MOCKS=true`? (Grep for any fetch() or axios calls outside of lib/integrations/ and lib/ai/)
- [ ] Does the seed script work cleanly on a fresh database? (`pnpm tsx scripts/seed.ts` on empty DB)
- [ ] Are all 3 businesses seeded?
- [ ] Are 48 reviews seeded (16 per business)?
- [ ] Do seeded reviews span all ratings 1-5?
- [ ] Do seeded reviews span both Google and Facebook sources?

```bash
# Check for any direct API calls outside the integration layer
grep -r "fetch(" app/ lib/ --include="*.ts" --include="*.tsx" | grep -v "lib/integrations" | grep -v "lib/ai" | grep -v "node_modules"
```

---

## LAYER 15 — Mobile & Responsive Layout

Check these viewports in browser DevTools (record pass/fail for each):

Viewports to test: 375px (iPhone SE), 390px (iPhone 14), 768px (iPad), 1024px (iPad landscape), 1440px (desktop)

For each viewport, check:
- [ ] No horizontal scroll on any page
- [ ] Navigation adapts correctly (hamburger or hidden links on mobile)
- [ ] Hero section stacks correctly
- [ ] All grids collapse to appropriate column counts
- [ ] All buttons are tap-friendly (minimum 44px height)
- [ ] Text is readable (no overflow or clipping)
- [ ] Demo card in hero is visible and not cut off
- [ ] Pricing cards stack on mobile
- [ ] FAQ accordion works on touch
- [ ] Live demo section usable on mobile
- [ ] Footer readable on mobile
- [ ] Activity ticker doesn't cover critical content on mobile

```bash
# Check for any fixed pixel widths that might cause mobile issues
grep -r "width: [0-9]*px" app/ components/ --include="*.tsx" --include="*.css" | grep -v "min-width\|max-width\|border\|height\|line-height"
```

---

## LAYER 16 — Performance & Load Behaviour

```bash
# Check bundle sizes from build output
pnpm build 2>&1 | grep -E "Route|Size|First Load"
```

Check and record:
- [ ] Is any route's First Load JS over 300KB? (Flag as 🟡 MAJOR — affects mobile load time)
- [ ] Is any route's First Load JS over 500KB? (Flag as 🔴 BLOCKER — will significantly hurt conversion)
- [ ] Are images using Next.js `<Image>` component for optimisation, or raw `<img>` tags?
- [ ] Are any external fonts loading from Google Fonts with proper `display=swap`?
- [ ] Does the landing page have a visible loading flash (FOUC) before styles apply?
- [ ] Does the demo card typewriter animation start without a visible delay?

---

## LAYER 17 — Security Surface Check

This is a mock-mode build so full security audit is not needed, but check these basics:

```bash
# Check for any secrets accidentally in code
grep -r "sk_live\|sk_test\|ANTHROPIC\|TWILIO\|RESEND" app/ lib/ --include="*.ts" --include="*.tsx" | grep -v ".env"

# Check .gitignore covers sensitive files
cat .gitignore | grep -E "\.env|secrets|\.key"
```

- [ ] No real API keys hardcoded anywhere in source files?
- [ ] `.env.local` is in `.gitignore`?
- [ ] API routes that modify data (POST/PUT/DELETE) check for authentication before processing?
- [ ] The `/api/dev/trigger` endpoint (manual Inngest trigger) — is it protected or open? An open dev trigger endpoint on a production server would be a 🔴 BLOCKER.
- [ ] Customer contact data (phone, email) — is it excluded from any console.log statements? (Check lib/inngest/send-review-request.ts specifically)
- [ ] Review reply posting — does it verify the business_id belongs to the authenticated user before posting? (Prevent one user from posting to another user's Google listing)

---

## LAYER 18 — Commercial Launch Readiness Verdict

After completing all layers above, compile your findings and answer these questions definitively:

### Q1: Is this app ready for commercial launch today?
Answer YES or NO. If NO, list the specific 🔴 BLOCKER issues that must be resolved first.

### Q2: What are the top 5 most critical issues found, in priority order?
For each: issue description, severity, which layer it was found in, proposed fix.

### Q3: What is missing that was specified in Product-Roadmap.md but not built?
List every feature or acceptance criterion from Product-Roadmap.md that is absent or broken.

### Q4: What was built that was NOT specified (scope creep or agent additions)?
List anything in the codebase that wasn't in the spec — this isn't automatically bad, but it should be documented.

### Q5: What are the top 3 UX issues most likely to cause a new user to abandon during onboarding?
Be specific — "user gets confused at step X because Y" not "onboarding could be improved."

### Q6: What is the estimated effort to fix all BLOCKER and MAJOR issues?
Rough estimate in development sessions (each session = one Cline autonomous run of ~2-4 hours equivalent).

### Q7: What are the 3 highest-impact improvements that could be made post-launch with minimum effort?
Things that would meaningfully improve conversion or retention but aren't launch-blocking.

### Q8: Is the MOCK-TO-REAL.md file complete and accurate enough to hand to a developer who hasn't seen this codebase before?
YES / NO / PARTIALLY — explain what's missing if not YES.

### Q9: Does the app's visual design match the approved landing-page.html reference?
Compare key elements: color palette, typography, component styles. Note any significant drift.

### Q10: Overall launch readiness score
Score from 1-10 where:
- 1-3 = significant rebuild required before any real user should see this
- 4-6 = core flows work but multiple important gaps remain
- 7-8 = ready for beta with known issues to fix during beta
- 9-10 = ready for commercial launch

---

## FINAL DELIVERABLE — Write AUDIT-REPORT.md

Create `AUDIT-REPORT.md` in the project root with this exact structure:

```markdown
# ReviewReply-Lite — Full Application Audit Report
**Date:** [today's date]
**Auditor:** GLM 5.2 (automated audit session)
**App version:** v0.1.0-mock
**Audit scope:** Full multilayer E2E, 18 layers

## Executive Summary
[2-3 sentence summary of overall state]

## Commercial Launch Readiness: [YES / NO / CONDITIONAL]
[One paragraph verdict]

## Overall Score: [X/10]

## Blocker Issues (must fix before launch)
[List all 🔴 findings]

## Major Issues (fix before commercial launch)
[List all 🟡 findings]

## Minor Issues (fix within first week post-launch)
[List all 🟠 findings]

## Polish Items (fix when time permits)
[List all 🔵 findings]

## Layer-by-Layer Findings
[Full findings for each of the 18 layers]

## Q&A Verdict (answers to all 10 commercial launch questions)
[Full answers to Q1-Q10]

## Recommended Fix Priority Order
[Numbered list: fix this first, then this, then this...]

## Estimated Sessions to Launch-Ready
[X autonomous Cline fix sessions estimated]
```

Do not write any application code during this session.
Do not make any fixes during this session.
Write only the audit report.
Commit the report:

```bash
git add AUDIT-REPORT.md
git commit -m "[Audit] Full multilayer E2E audit — commercial launch readiness assessment"
git push origin main
```